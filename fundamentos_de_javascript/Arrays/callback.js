 //funções callback são funções que chamam outras funções

 const nomes = ['lucas', 'maria', 'eduardo', 'geovana'];

 nomes.forEach (imprimeNome);

 function imprimeNome (nome) {console.log (nome)};